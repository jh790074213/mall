spring:
  application:
    name: gateway # 微服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 114.116.225.208:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: log-config.yaml
